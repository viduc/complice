{% extends 'layout/complice.html.twig' %}

{% block contenu %}
   {% include 'utilisateur/form.html.twig' %}

<div class="row">
    <div class="col-lg-12">
        <div class="bs-component">
            <div class="form-row align-items-center" >
                <div class="form-group col-lg-2">
                    {{ form_widget(form.save) }}
                </div>
                <div class="form-group col-lg-1"></div>
                <div class="form-group col-lg-2 custom-control custom-switch">
                    {{ form_widget(form.actif) }}
                    <label class="custom-control-label" for="user_actif">Actif</label>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>

{% endblock %}

